//Creating arrays
// To make an empty array
let students = [];

// An array of strings
let colors = ["red", "blue", "black"];

// An array of numbers
let lottoNums = [12, 17, 77, 88];

// A mixed array
let struff = [true, 68, "cat", null];

// Unlike other programming lenguajes, JS let us use any type
// of values inside arrays, no matter what.

let days = ["Monday", "Tuesday", "Wednesday"];
days.length;
// 3, referencing the 3 "things" inside the array
