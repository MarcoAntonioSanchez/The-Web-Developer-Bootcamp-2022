// Sort array method

// You can use it to sort numbers or strings.
// It's not a reliable method to sort numbers, for now and without
// getting into functions, this is the only example given so far:
let scores = [1, 70, 100, 2500, 9, -14, 0, 34];
scores.sort();
// -14, 0, 1, 9, 34, 70, 100, 2500
