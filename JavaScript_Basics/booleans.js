// Booleans

// True or false
// Booleans are very simple.
// You have two possible options: true or false. That's it.

let isLoggedIn = true;
let gameOver = false;
const isWaterWet = true;

// true and false are uppercase sensitive.
// In JS we must use all lowercase, for true and false values.

// Variables can change types:

let numPuppies = 23; // Number
numPuppies = false; // Now a boolean
numPuppies = 100; // Back to number

// It doesn't really make sense to change from a number to a
// boolean here, but we can!
